# 2026-03-26

Start: Rebuild transaction detail page with Reva as always-visible co-pilot.

DONE:
- Replaced `dealpilot-tn/web/src/app/transactions/[id]/page.tsx` with a clean new layout:
  - Top bar (address, badges, dates, actions)
  - Main content + permanent right-side Reva panel
  - Tabs: Overview, Documents, Checklist, Deadlines, Contacts, Comms
  - Removed any legacy UI elements like “Document Compliance”, “Download Audit Log”, “RF401 Wizard”, “Request Lender/Title…”, and “Deal Room”.
- Fixed routing:
  - Updated `dealpilot-tn/web/src/app/chat/page.tsx` so clicking a deal navigates to `/transactions/[id]` instead of mutating the URL to `/chat?deal=[id]`.
- Verified:
  - Search confirms banned legacy strings are not present in the new transactions detail page.
  - `next build` for the web app compiled successfully.


