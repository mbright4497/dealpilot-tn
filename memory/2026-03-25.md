2026-03-25

- Requested: explain codebase project structure (main pages, API routes) and authentication flow.
- Found Next.js App Router structure under `web/` with UI pages in `src/app/**/page.tsx` and API routes in `src/app/api/**/route.ts`.
- Auth uses Supabase: `src/middleware.ts` gates routes based on `supabase.auth.getUser()`; login uses email/password or Google OAuth; `/api/auth/callback` exchanges OAuth code for a session and redirects to `/onboarding` when `user_metadata.onboarded` is false.
- Key API examples: `/api/profile` (auth-gated profile read/update), `/api/transactions` (list/create/delete with optional `user_id` filtering), `/api/deadlines/generate` (writes `deal_deadlines`), and `/api/ghl/webhook` (signature-verified webhook inserts deals + triggers deadline generation).
- Fix: removed stray `*** End Patch` marker from `web/src/app/api/transactions/route.ts` (syntax error risk).
- Verification: `next build` succeeded; `next lint` still fails due to pre-existing lint rule violations across many files (not from this change).
- Additional fix for runtime 500: baked `WATCHPACK_POLLING=true` into `dealpilot-tn/web/package.json` `dev` script to avoid dev watcher `EMFILE` issues.
- Added webpack `watchOptions` polling in `dealpilot-tn/web/next.config.js` (defensive).
- Verification: fresh dev servers on ports `3004`, `3007`, `3008` returned `307` on `/` (redirect to `/login`) and `200` on `/chat`. Port `3000` remains 500 due to an already-running stale dev server that we can't restart in this sandbox.
- Task: renamed `Eva` -> `Reva` in `web/src/app/chat/page.tsx` (alt text) and `web/src/components/TransactionDetail.tsx` (`/api/eva/chat` endpoint + `EVA HERO` comment).
- Verification: re-ran searches and confirmed no remaining `Eva`, `/api/eva`, or standalone `EVA` matches under `web/src`.

- Supabase Google OAuth prod debug: excluded `/api/auth/callback` from `middleware.ts` matcher (belt-and-suspenders with early return); normalized pathname for trailing slash; migrated `createServerSupabaseClient` in `lib/supabase/server.ts` from deprecated `get`/`set`/`remove` to `getAll`/`setAll` so PKCE code-verifier + chunked session cookies are fully read/written per `@supabase/ssr` behavior. `npm run build` OK.

- Google OAuth prod fix (exchange fails): root cause was `/api/auth/callback` using `cookies()` from `next/headers` via `createServerSupabaseClient()` — PKCE verifier cookies set by the browser were not reliably visible during `exchangeCodeForSession` on Vercel. Switched callback to `createMiddlewareSupabaseClient(request)` (request-bound `getAll`/`setAll` like Remix’s `parseCookieHeader`) and `forwardCookies` onto `NextResponse.redirect` so session `Set-Cookie` survives the redirect. `npm run build` OK.

- Password reset fix: `resetPasswordForEmail` used `redirectTo` → `/reset-password`, but PKCE recovery adds `?code=` — no `exchangeCodeForSession` ran, so `updateUser` had no session. Changed `redirectTo` to `/api/auth/callback?next=/reset-password`; callback now forces `next=/reset-password` over onboarding. `reset-password` page also exchanges `?code=` if present (legacy links) and validates session before form submit.

- Auth callback debugging: cannot read Vercel runtime logs from this environment. Added staged `step=1..16` `[auth/callback]` logs (query params, cookie names only, exchange/getUser/redirect). Commit `e1ae1e48`.
- Google OAuth + password reset still failing with `Unable to exchange external code`. Adjusted browser cookie policy for Supabase PKCE storage:
  - `src/lib/supabase-browser.ts`: when on HTTPS, set `sameSite: "none"` and `secure: true` for auth cookies so the PKCE code verifier survives cross-site OAuth redirects.
  Commits: `a13d97dd`, `e466fc00`, `b6242abe`.

- Auth prod fix pass: aligned Supabase `redirectTo` values to match the allowlisted callback URL exactly (removed `?next=...` for Google OAuth + password reset). Updated `/api/auth/callback` to route password-recovery landings to `/reset-password` based on recovery-like query params, and switched callback to the same middleware-bound Supabase client. Fixed `forwardCookies` to copy cookie options correctly and removed request cookie mutation from the middleware Supabase client.
- Verification: `dealpilot-tn/web` `next build` succeeds after the auth changes.
